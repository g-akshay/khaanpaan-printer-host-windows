# Web Store submission build outputs.
# Generated by `pnpm run build:store`; per-version zip uploaded to the
# Chrome Web Store dev console. Don't commit these — they're reproducible
# from manifest.prod.json + the runtime files.
/web-store/khanpan-printer-connector-*.zip
/web-store/.stage/
